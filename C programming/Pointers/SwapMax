QUESTION: Write a function min_max_swap that accepts five addresses which contain five distinct integer elements.
The function swaps the location of minimum and maximum elements.

CODE:
#include<stdio.h>
void min_max_swap(int*a,int*b,int*c,int*d,int*e){
    
    int *maximum= a;
    int *minimum= a; 
    
    if (*b>*maximum){
        maximum= b;
    }
    if (*c>*maximum){
        maximum= c;
    }
    if (*d>*maximum){
        maximum= d;
    }
    if (*e>*maximum){
        maximum= e;
    }
    
    
    
    if (*b<*minimum){
        minimum= b;
    }
    if (*c<*minimum){
        minimum= c;
    }
    if (*d<*minimum){
        minimum= d;
    }
    if (*e<*minimum){
        minimum= e;
    }
    
    //Now, We have minimum and maximum.Start swapping.

     int temp;
     temp= *minimum;
     *minimum= *maximum;
     *maximum= temp;
}
int main()
{
    int a,b,c,d,e;
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    min_max_swap(&a,&b,&c,&d,&e);
    printf("%d %d %d %d %d",a,b,c,d,e);
}
